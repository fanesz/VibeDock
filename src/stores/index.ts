// Feature stores. VibeDock state is local (Zustand + persist over localStorage).
export { useWorkspace, deriveRecents } from "./workspace";
export { useTerminals } from "./terminals";
export { useUI } from "./ui";
